package validator;

import model.Teacher;

public class Mapper {
    
    public boolean Validate (Object object) {

        if (object instanceof Teacher) {
            
        }

         return true;
    }
}

package validator;

import entity.Teacher;

public class Mapper {
    
    public boolean Validate (Object object) {

        if (object instanceof Teacher) {
            
        }

         return true;
    }
}
